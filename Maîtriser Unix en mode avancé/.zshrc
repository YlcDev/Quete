
export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting

# export PATH=$HOME/local/bin:$PATH >> ~/.bashrc
source .bashrc

# ci dessous la ligne venant de .bashrc ligne du dessus = original 
export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting
